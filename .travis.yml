language: python
python:
- '3.6'
install:
- pip install ansible==2.5.5 molecule apache-libcloud
script:
- molecule --debug test
after_script:
- molecule --debug destroy
before_install:
# [START auth]
# Decrypt the credentials we added to the repo using the key we added with the Travis command line tool
# If the SDK is not already cached, download it and unpack it
#e we use the decrypted service account credentials to authenticate the command line tool
#- gcloud auth activate-service-account --key-file infra1-234210-9ac6f3fcb9d4.json
# [END auth]
- openssl aes-256-cbc -K $encrypted_111cffa1dd2d_key -iv $encrypted_111cffa1dd2d_iv
  -in credentials.tar.gz.enc -out credentials.tar.gz -d

notifications:
  slack: nonamed-hq:oNSDgpO1cSanENRp1cXPmINc
